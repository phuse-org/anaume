% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jpn_query_base.R
\name{jpn_query_base}
\alias{jpn_query_base}
\title{Create ARD (analysis ready data) for adverse event summaries}
\usage{
jpn_query_base(
  data,
  denominator,
  variables = NULL,
  by = dplyr::group_vars(data)
)
}
\arguments{
\item{data}{A data.frame that must contain at least \code{USUBJID} and \code{AETYPE}.
Typically ADAE expanded to multiple rows per subject/event type.}

\item{denominator}{A data.frame used as the denominator in \code{{cards}}.
Must contain the columns specified in \code{by} (if provided).}

\item{variables}{Optional tidyselect specification of additional variables to
summarize hierarchically under \code{AETYPE}. If \code{NULL} (default), \code{variables}
will be \code{AETYPE} only.}

\item{by}{Optional tidyselect specification of column(s) used for column splits
(e.g. treatment arm, subgroup). If \code{NULL} (default), no \code{by} columns are used.}
}
\value{
A \code{{cards}} ARD object returned by \code{cards::ard_stack_hierarchical()}.
}
\description{
Helper to create a \code{{cards}} ARD based on an ADAE-like dataset that has already
been expanded with \code{AETYPE} (e.g. via \code{bind_by_aetype()} upstream).
}
\details{
The ARD is created by \code{cards::ard_stack_hierarchical()} with:
\itemize{
\item subject identifier fixed to \code{USUBJID}
\item \code{AETYPE} always included in \code{variables}
\item optional additional summary variables (\code{variables})
\item optional stratification columns (\code{by}) such as treatment arm and subgroup factors
}

This function assumes \code{data} already includes \code{AETYPE}; it does not call
\code{bind_by_aetype()} internally.

Internally, \code{variables} is resolved to column names from \code{data} and combined as:
\code{unique(c("AETYPE", variables))}.
}
\examples{
\dontrun{
# adae_aetype <- bind_by_aetype(adae, ae_types)
ard <- jpn_query_base(
  data = adae_aetype,
  denominator = adsl,
  by = TRT01A
)

ard_pt <- jpn_query_base(
  data = adae_aetype,
  denominator = adsl,
  by = TRT01A,
  variables = AEDECOD
)
}

}
\keyword{internal}
