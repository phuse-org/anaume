% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jpn_query_aggregate.R
\name{jpn_query_aggregate}
\alias{jpn_query_aggregate}
\title{Create ARD (analysis ready data) for adverse event summaries (aggregate wrapper)}
\usage{
jpn_query_aggregate(data, denominator, ae_types, variables = NULL, by = NULL)
}
\arguments{
\item{data}{A data.frame (ADAE-like). Unlike \code{\link[=jpn_query_base]{jpn_query_base()}}, \code{data} does not
need to contain \code{AETYPE} in advance.}

\item{denominator}{A data.frame used as the denominator in \code{{cards}}.
Must contain the columns specified in \code{by} (if provided).}

\item{ae_types}{List of \code{ae_type} objects passed to \code{bind_by_aetype()} to create \code{AETYPE}.}

\item{variables}{Optional tidyselect specification of additional variables to
summarize hierarchically under \code{AETYPE}. If \code{NULL} (default), \code{variables}
will be \code{AETYPE} only.}

\item{by}{Optional tidyselect specification of column(s) used for column splits
(e.g. treatment arm, subgroup). If \code{NULL} (default), no \code{by} columns are used.}
}
\value{
A \code{{cards}} ARD object returned by \code{cards::ard_stack_hierarchical()}.
}
\description{
Wrapper around \code{\link[=jpn_query_base]{jpn_query_base()}} that first expands an ADAE-like dataset with
\code{AETYPE} using \code{bind_by_aetype()} and then creates a \code{{cards}} ARD via
\code{cards::ard_stack_hierarchical()}.
}
\details{
Use this function when you have a raw ADAE dataset (without \code{AETYPE}) and an
\code{ae_types} specification, and want to obtain the same ARD output as
\code{\link[=jpn_query_base]{jpn_query_base()}} in a single call.

Internally this function performs:
\enumerate{
\item \code{bind_by_aetype(data, ae_types)} to add \code{AETYPE}
\item \code{\link[=jpn_query_base]{jpn_query_base()}} with \code{data} replaced by the bound data
}
}
\examples{
\dontrun{
ard <- jpn_query_aggregate(
  data = adae,
  denominator = adsl,
  ae_types = ae_types,
  by = TRT01A
)

ard_pt <- jpn_query_aggregate(
  data = adae,
  denominator = adsl,
  ae_types = ae_types,
  by = TRT01A,
  variables = AEDECOD
)
}

}
